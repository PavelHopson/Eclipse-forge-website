# Handoff — 2026-05-07 → 2026-05-08

> Скопируй этот текст как **первое сообщение** в новый чат с Claude Code.
> Если работаешь через VS Code Claude Code или Claude Code CLI в репо
> `E:\projects\EclipseForgeLanding`, файл `CLAUDE.md` подхватится
> автоматически — этот handoff даёт _свежую_ операционную сводку.

---

## Сообщение для нового чата (копи-пасть)

```
Привет, продолжаем работу над Eclipse Forge.

Контекст: ты Claude Opus 4.7 (1M context), помогаешь Pavel Hopson'у
с портфолио-сайтом https://pavelhopson.github.io/Eclipse-forge-website/
(репо https://github.com/PavelHopson/Eclipse-forge-website,
локально E:\projects\EclipseForgeLanding).

Прочитай в первую очередь:
1. CLAUDE.md в корне репо — это твоя ориентация (бренд-правила,
   стек, текущее состояние, work style)
2. docs/kworks.md — 17 услуг Pavel'а в kwork-формате
3. docs/cover-prompts.md — visual style для project covers (v2 luxury gold)
4. docs/imagery-prompts.md — все остальные imagery prompts

Что сделано вчера (07.05):
- Подключён logo.png и founder-operator.png (фикс дубликации портрета)
- Создан docs/kworks.md (17 service offerings)
- Починено мерцание изображений на mouse move (GPU acceleration +
  rAF throttle + early bail на touch)
- Mobile/tablet адаптация (hero core, modal, telemetry HUD)
- Отдельной волной разбирали kwork.ru формы — Pavel оформил 3 кворка
  (Valhalla, Sentinel, Premium Rent) интерактивно через скриншоты

Где остановились:
Pavel в процессе оформления кворков на kwork.ru. Закончил 3 из 17.
Следующие в очереди (Tier 1):
- Task Manager — fullstack JWT платформа
- WireGuard Telegram bot — VPN provisioning
- retry-http — npm reliability module

Текущий формат работы по кворкам: Pavel кидает скрин формы, ты
выдаёшь готовые тексты под все блоки (название, описание, объём,
цена, срок, 8-9 допов с подсказками, FAQ x5). Адаптация моделей:
- На kwork.ru → база 5-15k ₽ + допы 1.5-8k ₽ за каждый
- 30%+ текста на русском в каждом названии допа (kwork блокирует
  иначе)
- Подсказки для покупателя обязательны — влияют на конверсию

Сегодня хочу: [допиши какой кворк делаем дальше / какой другой
вопрос — design / imagery / etc.]
```

---

## Что в backlog (чтобы помнить)

### Design / Tech
- WebGL/Three.js eclipse в hero (отложено)
- Lenis smooth scroll + GSAP ScrollTrigger
- Forge Core signature asset (Tier E #12 в imagery-prompts.md)
- Favicon набор + apple-touch-icon
- OG image v2 (1200×630)
- Полный storytelling rename секций
- Footer brand watermark

### Marketplace
- 14 оставшихся kworks (Tier 1: Task Manager / WG Bot / retry-http;
  Tier 2: 9 AI-продуктов; Tier 3: Action OS, Discipline OS и т.д.)

---

## Quick Reference — что где лежит

| Что нужно | Файл |
|---|---|
| Цены, услуги, контакты | `src/data/content.ts` |
| Hero compositioning | `src/components/sections/HeroSection.tsx` |
| Price modal | `src/components/ui/PriceListModal.tsx` |
| Cursor light | `src/components/ui/CursorLight.tsx` |
| Telemetry HUD | `src/components/ui/EclipseTelemetryHud.tsx` |
| Все CSS | `src/styles/index.css` |
| Промпты обложек | `docs/cover-prompts.md` |
| Промпты imagery | `docs/imagery-prompts.md` |
| Тексты услуг | `docs/kworks.md` |
| Brand rules / state | `CLAUDE.md` (корень) |

---

## Backup / Rollback

Если что-то пойдёт не так в новой сессии:

```bash
cd E:\projects\EclipseForgeLanding
git log --oneline -10                              # увидеть последние коммиты
git tag --list | grep cinematic                    # увидеть backup tags
git checkout pre-cinematic-2026-05-04 -- src/     # откатить только src
git reset --soft HEAD~1                            # откатить последний коммит
```

Текущий стабильный коммит: `da1ffc4` (fix flicker + mobile pass).
Последний tag: `pre-cinematic-2026-05-04` (точка перед luxury gold pivot).

---

_Generated 2026-05-07 by Claude Opus 4.7 (1M context)_
